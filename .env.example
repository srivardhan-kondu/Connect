# Server-only secret for the CONNECT Assistant (src/app/api/chat/route.ts).
# Copy this file to .env.local and paste your key from https://console.anthropic.com/settings/keys
# Never prefix it with NEXT_PUBLIC_: that would ship the key to every visitor.
ANTHROPIC_API_KEY=
